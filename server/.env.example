HOST=0.0.0.0
PORT=8081
DEBUG=false
# API key required by clients in the X-API-Key header (or Authorization: Bearer).
# Generate one with: openssl rand -hex 32
SECRET=change-me
LOG_LEVEL=info
DB_PATH=data/espspotify.db
STORAGE_PATH=storage